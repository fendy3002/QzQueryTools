<head>{
	description: "Testing sql query",
	params: {
		"name" : "varchar",
		"id" : "int",
		"list" : "%"
	},
	availableTo: ["mysql"]
}</head>

<Q i=1>
SELECT 1 as `col1` UNION ALL
SELECT 2 as `col1`;
</Q>

<Q i=2>
SELECT @id as `col2`;
</Q>

<Q i=3>
SELECT @name as `col3`;
</Q>